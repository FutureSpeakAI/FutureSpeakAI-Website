## Packages
framer-motion | Subtle minimal page animations and transitions

## Notes
The application has a minimalist design style focusing on crisp typography and whitespace.
The backend provides a single config endpoint at `/api/config` to fetch site details.
